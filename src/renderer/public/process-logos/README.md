# Process Logos

Drop process logo images in this folder.

Supported formats:
- SVG (.svg)
- PNG (.png)

Renderer paths should use root-relative URLs, for example:
- /process-logos/postgresql.svg
- /process-logos/redis.png

Current code includes default logoPath values for common process ids. You can replace files or rename them and update the corresponding logoPath values in process definitions.
